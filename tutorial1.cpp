#include <iostream> // library
using namespace std; // namespace - Declarative region that provides a scope to the identifiers (the names of types functions, variables, etc) inside it. (shorthand std:: instead of the using namespace std;)
int main() {
    std::cout << "Hello World!";
    return 0;
}